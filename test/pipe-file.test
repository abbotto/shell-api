#!/usr/bin/env sh

. shell/print-text
. shell/terminate
. shell/pipe-file

echo ""
print-text 12 "Checking if the 'pipe-file' function works..."

echo ""
pipe-file ./tmp/.env.* cat {}

terminate "${?}" "PASS" "FAIL"
